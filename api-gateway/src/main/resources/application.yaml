server:
  port: 8090
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          # 路由规则
          routes:
            # 示例接口服务路由
            - id: api-demo-service
              uri: http://localhost:9003  # 你的业务接口地址
              predicates:
                - Path=/api/**
              filters:
                - StripPrefix=1


          # 全局跨域配置
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOrigins: "*"
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - DELETE
                allowedHeaders: "*"
                allowCredentials: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    # 连接超时时间（毫秒）
    connection-timeout: 60000
    # 心跳间隔（秒），防止连接超时断开
    requested-heartbeat: 30
    # 自动恢复连接
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 1000
          max-attempts: 3
          max-interval: 10000
        acknowledge-mode: auto
        prefetch: 1
    # 定义队列名称（常量）
    exchange:
      log: api.call.log.exchange
    queue:
      log: api.call.log.v2.queue
      blackList: api.black.list.queue
    routing:
      log: api.call.log

  # 数据库
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/api_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    hikari:
      maximum-pool-size: 30
      minimum-idle: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      validation-timeout: 5000

  # Redis
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10000
      lettuce:
        pool:
          max-active: 32
          max-wait: -1
          max-idle: 16
          min-idle: 8

# MyBatis-Plus
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

api:
  gateway:
    sign-expire: 5
    default-limit-count: 100
    default-limit-period: 60
    limit-dimension: accessKey
    auth:
      base-url: http://localhost:9002
      timeout-ms: 1500
    cache:
      app-auth-ttl-seconds: 300
      interface-ttl-seconds: 300

resilience4j:
  circuitbreaker:
    instances:
      adminAuth:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 20
        minimum-number-of-calls: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3